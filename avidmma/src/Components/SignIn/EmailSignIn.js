import React from 'react'
import { Button } from '@mui/material'

const EmailSignIn = () => {
    return (
        <Button sx={{ m: "15px auto", width: '80%' }} variant='contained'> SIGN IN</Button>
    )
}

export default EmailSignIn